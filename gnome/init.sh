# Non so ancora dove mettere questa parte

# Attiva bluetooth
sudo systemctl enable --now bluetooth

# Swap caps lock with escape
gsettings set org.gnome.desktop.input-sources xkb-options "['caps:swapescape']"
